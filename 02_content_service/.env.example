# Copy these values into the integrated project root .env or export them in your shell.
# When launched inside the sibling-folder project layout, run_local.py reads the root .env automatically.
CONTENT_SERVICE_PORT=38410
LOCAL_DATA_DIR=./local_data
CONTENT_SERVICE_IMPORT_WORKERS=2
LIBREOFFICE_BIN=soffice
CONTENT_SERVICE_VERSION=1.0.0
